name: youtube_app
description: SayuraTube App

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.0
  local_auth: ^2.1.8
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
